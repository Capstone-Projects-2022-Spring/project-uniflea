import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
        alignItems: 'center'
    },
    ms: {
        width: '80%',
        padding: 3,
        margin: 3,
    },
    result: {
        padding: 1,
    }

});

export default styles;