import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
        alignItems: 'center'
    },
    ms: {
        width: '100%',
        padding: 5
    },
    result: {
      padding: 5
    }

});

export default styles;